import psycopg2
import pandas as pd

conn = psycopg2.connect("""
    host=rc1a-5v1hh8zp1edg6u8j.mdb.yandexcloud.net
    port=6432
    sslmode=verify-full
    dbname=mansi_russian_translator
    user=smynko
    password=cie98vxu
    target_session_attrs=read-write
""")

print(pd.read_sql('SELECT * FROM pg_catalog.pg_tables', conn=conn))
q = conn.cursor(), 
q.execute('SELECT * FROM pg_catalog.pg_tables')

print(q.fetchone())

conn.close()

